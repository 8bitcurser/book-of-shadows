package components

templ FormHeader(title string, description string) {
    <div class="mb-4 text-center">
        <h2 class="fw-bold mb-3 form-section-header">{title}</h2>
        <p class="text-secondary mb-4">{description}</p>
    </div>
}